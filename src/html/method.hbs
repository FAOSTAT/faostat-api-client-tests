FAOSTATAPIClient.prototype.{{rel}} = function(config) {
    $.ajax({
        url: {{{url}}},
        data: data,
        type: {{{method}}},
        success: config.success,
        error: config.error
    });
};